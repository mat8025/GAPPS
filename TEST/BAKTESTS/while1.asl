///
///
///


checkin()
N = 10

int k = 0

 while ( k < N)  k++;

<<" DONE $k $N \n"

checkNum(k,N);

 k =0;
 int m = 0;
 while ( k++ < N) {
   m++;
   <<" $k $m \n"

 }

<<" DONE $k $N \n"
checkNum(k,N+1);
 m = 0;
 k = 0;
 while ( ++k < N) {
   m++;
   <<" $k $m \n"
 }

checkNum(k,N);
<<" DONE $k $N \n"

checkOut()


///////////////////////////////
