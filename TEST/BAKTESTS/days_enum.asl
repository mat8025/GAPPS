

///////////////////////
// enum is done on the first pass

<<"enum is done on the first pass \n"

enum  days 
        { 
             MON = 1, 
             TUE,  // should be 2 
             WED, 
             THU, 
             // nearly done
             FRI, 
             SAT,
             SUN, 
             FUN = 80, 
        };  // days of the week


<<" $(typeof(days))  $(Caz(days))\n"


///////////////////////