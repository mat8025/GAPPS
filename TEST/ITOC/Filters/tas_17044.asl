/// 
//// asl filter code

Svar Ip;
Svar F;
fs= '"';
  while (1) { 
      Ip=readline(); 
      if (feof()) break; 
      F= Split(Ip,fs); 


/// code insert  

    nm= F[0]; <<"$nm\n"; 


/// end code insert  

 } /// end filter  

