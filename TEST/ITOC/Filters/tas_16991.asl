/// 
//// asl filter code

Svar Ip;
Svar F;
fs=;
  while (1) { 
      Ip=readline(); 
      if (feof()) break; 
      F= Split(Ip,fs); 


/// code insert  

    nm= Scut(F[0],4); <<"$nm"; 


/// end code insert  

 } /// end filter  

