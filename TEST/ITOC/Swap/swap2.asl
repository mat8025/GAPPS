setdebug(1)

proc swap ( &x, &y)
{
  t = 0

<<"$_proc IN : %V$x $y $t\n"

  t = x
  x = y
  y = t

<<" OUT: %V$x $y $t\n"

}


 int k = 4
 int m = 3

  swap(1,2)
ans += atoi(i_read('pass? :\ '))
<<"%V$ans\n"


<<"%V$k $m  ref\n"
 swap (k, m)
<<" %V$k $m \n"
ans += atoi(i_read('pass? :\ '))
<<"%V$ans\n"

<<"%V$k $m  ref\n"
 swap (k, m)
<<" %V$k $m \n"
ans += atoi(i_read('pass? :\ '))
<<"%V$ans\n"