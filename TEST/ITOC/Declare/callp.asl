


#include "debug";

chkIn()

debugON();

chkIn()

proc Goo(int a, int b)
{
 int k = 2
<<" entered $_proc single arg %V$a $k $A\n"
 chkN(k,2)
 
 chkN(a,A)
 return k;
}


proc Foo(int a, int b)
{
<<" $_proc 2 args $a $b\n"
  int x;
  int y = 1;
  y->info(1);
  chkN (y ,1)
  x = a;
  y = b;

  c= x+ y;
 chkN(a,m)
<<"%V $c $a $b $x $y \n"

 // return c;
}
//======================================//


int m = 3;
int n = 4;





// pri = Foo(m,n)

  Foo(m,n)
 m++;
 n += 3;
 
 Foo(m,n)



A =5
k2=Goo(A,7)

A++
k2=Goo(A,8)



chkOut()