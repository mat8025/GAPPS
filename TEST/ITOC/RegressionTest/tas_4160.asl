/// 
//// asl filter code

Svar ipl;
Svar Fs;
  while (1) { 
      ipl=readline(); 
      if (feof()) break; 
      Fs= Split(ipl); 


/// code insert  

    nm=scut(Fs[0],2); <<"$nm\n"; 


/// end code insert  

 } /// end filter  

