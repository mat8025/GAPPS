   allowErrors();
   ws = getScript(); 
   <<"%V $ws\n"; 
int myele = 47;
int N= 0;
   na= argc();
   if (na >1) {
    N = atoi(_clarg[1])
    <<"$N set from command line\n";
   }
   if (N == 80) {
<<"this is Hg \n";
    myele = 80;
    }
   else if (N == 79) {
<<"this is Au \n";
    myele = 79;
    }
exit();
