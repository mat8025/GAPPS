echo "bash args" $*

asl -d -s 'Svar z=_clarg[1:-1:1]; Str sa="$z"; opendll("uac");  show_task(sa); exit(0); ' $*

#asl -d -s 'Svar z=_clarg[1:-1:1]; Str sa="$z"; <<"%V $sa\n"; opendll("uac"); test_glines(sa); exit(0); ' $*
