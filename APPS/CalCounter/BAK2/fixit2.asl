
int fit = 0;

int match ;
int charrem;

wd= "CHICK"

phr2 = "CHICKEN SOUP"

  spat(phr2,wd,0,1,&match,&charrem);


    if (match) {
       fit = 30;
        <<"$match $wd $phr2 $fit\n"
     ans=iread()
   }

    if (match >= 1) {
       fit = 20;
        <<"$match $wd $phr2 $fit\n"
     ans=iread()
   }
